<h1>Welcome to the Cluedo game</h1>
<p>Your submission will be added to the database. Refresh after submission to see it.</p>

<%= form_with(url: "/msg", method: "post") do %>
  <%= label_tag(:q, "Type your message") %>
  <%= text_field_tag(:q) %>
  <%= submit_tag("Submit") %>
<% end %>
<br>
<h2>Entries</h2>
<% Dice.all.each do |f| %>
  <%= f.dice %>
  <br>
<% end %>